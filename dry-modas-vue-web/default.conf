server {
    listen 80;

    server_name localhost;

    charset utf-8;
    root /usr/share/nginx/html;
    try_files $uri /index.html;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}